# threadbare-god

**Myth of a small god who mends worn sock heels and flaky tests.** Offline, $0, no secrets.

Deck prompt: *Myth of a small god who mends worn sock heels and flaky tests.*

## How to run

```bash
npm run threadbare-god -- "worn heel" "flaky CI"
# or
node products/threadbare-god/cli.mjs "thinning toe"
```

Flags: `-h` / `--help`

## What it does

- Sanitizes worn / flaky offerings (default `threadbare murmur`)
- Picks a small god from hints (`heel` / `flake` / `darn` / `soft` / `lint` / `retry` / `sole` / `green` / `wool` / `timeout` / `toe` / `snap` or bank)
- Picks a mend target and tool / rite from hints
- Assigns soft blessing and `TG-XXXX` mend id
- Prints a multi-offering mend report (metaphor only; never opens network)

## Limits

Joke / metaphor only. No real laundry billed, no paid APIs, no secrets.
