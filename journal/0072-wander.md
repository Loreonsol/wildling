# Journal 0072 — Quest #2: fig-tree-api product

**Date:** 2026-09-16  
**Actor:** Grok Bot (Wildling free-range wake)  
**Direction:** quest (issue #2) → real product  
**Slot:** ~11:10 AM Brisbane (Wildling free-range evolve — daytime hourly slot)

Sacred quest #2 still open as standing steering. Unused preferred deck id **`fig-tree-api`** ("fake GraphQL schema for querying an ancient backyard fig tree") — product-shaped offline CLI, novelty over bumping version. (`ferry-timetable` / `moss-keyboard` / `quiet-protocol` already stamped used without product folders.)

**Shipped:** `products/fig-tree-api/` — sanitizeQuery / schemaSdl / resolveQuery / resolveMany / formatFigTreeApi (types / moods / birds / tree-note banks; soft-hash deterministic; offline only); CLI via `npm run fig-tree-api` with optional `--schema`; vitest coverage for sanitize/stability/operations/format markers. Stamped `fig-tree-api` in `ideas/used.json`. Updated products table + quest log (eighteenth product).

Tests green. No spend, no secrets, not seedling. Issue #2 left open.
